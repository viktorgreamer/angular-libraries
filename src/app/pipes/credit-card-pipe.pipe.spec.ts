import { CreditCardPipePipe } from './credit-card-pipe.pipe';

describe('CreditCardPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CreditCardPipePipe();
    expect(pipe).toBeTruthy();
  });
});
